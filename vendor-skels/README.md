This directory contains adevtool-generated text files from vendor modules for every supported device.

These files are stored here to record their changes to Git history. They are not used for vendor module generation.

Note that .mk and .bp files have a .skip suffix added to their names to prevent Soong from detecting them,
which would break the build.
